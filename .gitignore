
# Node
node_modules/
npm-debug.log
yarn-error.log

# Segredos
.env

# Documentação e scripts auxiliares
md/

# Uploads temporários do painel web
server/.tmp-uploads/
server/.tmp-extract/

# Saída do mapa web (squaremap): milhares de tiles gerados por instalação
server/plugins/squaremap/web/
server/plugins/squaremap/data/

# Cache das estruturas encontradas pelo /locate (varia por seed/instalação)
server/.structures-cache.json

# Ícones extraídos do Minecraft (texturas da Mojang: não redistribuímos no repo).
# Gere com: bash extract-map-icons.sh
public/map-icons/

# Builds do app desktop (grandes demais para o GitHub; gere com npm run build:linux/win)
electron-app/dist/
electron-app/node_modules/
builds/**
!builds/LEIA-ME.md
